Dear <%=raw( @member && @member.name || 'member' )%>,

a new decision has been made.

<%=raw( @proposal.title )%>, by <%=raw( @proposal.proposer.name || @proposal.proposer.email )%>

<%=raw( @proposal.description )%>

<%=raw( decision_url(@proposal.decision) )%>

Thanks

<%=raw @organisation_name%>
Powered by One Click Orgs
