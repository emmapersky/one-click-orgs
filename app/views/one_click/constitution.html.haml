%h2 Constitution

%p
  %span.dynamic
    Highlighted clauses
  can be changed in
  = link_to 'Settings', settings_path

- if current_organisation.pending? or current_organisation.active?
  =render :partial => 'shared/constitution', :locals => {:allow_changes => true}

- if current_organisation.proposed?
  =render :partial => 'shared/constitution', :locals => {:allow_changes => false}
