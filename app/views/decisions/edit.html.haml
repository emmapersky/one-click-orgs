%h1
  Decisions controller, edit action
%p
  Edit this file in
  %tt
    app/views/decisions/edit.html.erb
= error_messages_for @decision

= form_for(@decision, :action => url(:decision, @decision)) do
  %p
    = submit "Update"

= link_to 'Show', url(:decision, @decision)
|
= link_to 'Back', url(:decisions)